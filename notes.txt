gameState = {
  players: [
    {
      userId,
      name,
      cards,
      score
    }

  ],
  playingCards: [
  ],
  currentTurn: userId,
  game: 0,
  round: 0,
}

Create User database
  login mechanism
  token mechanism

Figure out disconnect mechanism using keeping track of socket id